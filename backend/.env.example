# Backend environment variables

# Option 1: Full URL (recommended)
# For the provided database container on port 5001:
DATABASE_URL=postgresql+psycopg2://appuser:dbuser123@localhost:5001/myapp

# Option 2: Discrete PG variables (used if DATABASE_URL is unset)
PGHOST=localhost
PGPORT=5001
PGDATABASE=myapp
PGUSER=appuser
PGPASSWORD=dbuser123

# API port (optional)
APP_PORT=8000

# Optional: CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
