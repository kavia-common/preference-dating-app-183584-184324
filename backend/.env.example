# Environment variables for FastAPI backend

# Preferred: Single connection URL (psycopg2 driver)
# Default points to the preview PostgreSQL on port 5001
DATABASE_URL=postgresql+psycopg2://appuser:dbuser123@localhost:5001/myapp

# Alternatively, use discrete PG* variables (if DATABASE_URL is empty, these are used)
PGHOST=localhost
PGPORT=5001
PGDATABASE=myapp
PGUSER=appuser
PGPASSWORD=dbuser123

# App configuration
APP_PORT=8000

# Optional CORS (comma-separated origins)
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
