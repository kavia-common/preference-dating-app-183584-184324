# Database (use DATABASE_URL if available)
DATABASE_URL=postgresql://appuser:dbuser123@localhost:5001/myapp
# Or provide discrete values
# PGHOST=localhost
# PGPORT=5001
# PGDATABASE=myapp
# PGUSER=appuser
# PGPASSWORD=dbuser123

# Frontend origin (CORS)
FRONTEND_URL=http://localhost:5173

# API port (for docs route message only)
API_PORT=8000
