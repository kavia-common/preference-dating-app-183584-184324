# Database connection settings for test suite
# Default local preview DB is on TCP port 5001 per container startup.sh
PGHOST=localhost
PGPORT=5001
PGDATABASE=myapp
PGUSER=appuser
PGPASSWORD=dbuser123

# Alternatively specify a full connection string (overrides above)
# DATABASE_URL=postgresql://appuser:dbuser123@localhost:5001/myapp
